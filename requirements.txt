# Replit-Optimized requirements.txt
# This configuration EXCLUDES heavy libraries (torch, sentence-transformers)
# to stay within Replit's 1-2GB disk quota, relying on the project's
# built-in local embedding fallback logic.

# Core Web Framework
Django
gunicorn
whitenoise
dj-database-url
psycopg2-binary
python-dotenv

# Search & Vector Space (Lightweight)
numpy
faiss-cpu==1.9.0

# API-Based Intelligence
openai
groq
requests
pydantic

# File Formatting
PyPDF2
python-docx
lxml
