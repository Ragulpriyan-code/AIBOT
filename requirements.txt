# FINAL ULTRA-LIGHTWEIGHT REQUIREMENTS
# (Crucial: Removes all heavy AI libraries that cause Disk Quota errors)

# Core Web Framework
Django
gunicorn
whitenoise
dj-database-url
psycopg2-binary
python-dotenv

# API Tools (Lightweight)
openai
groq
requests
pydantic

# Simple File Processing
PyPDF2
python-docx

# NOTE: Removed 'torch', 'sentence-transformers', 'transformers', and 'faiss'.
# These libraries are 2GB+ and cause Disk Quota Exceeded errors.
# The chatbot will now use pure API-based responses.
